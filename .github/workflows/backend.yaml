name: Backend CI/CD (GKE + Helm + GCP)

on:
  push:
    branches:
      - main

env:
  PROJECT_ID: model-nexus-496108-s7
  GKE_CLUSTER: mern-gcp-dev-gke
  GKE_ZONE: europe-west1-b
  REGION: europe-west1
  ARTIFACT_REGISTRY: europe-west1-docker.pkg.dev
  REPOSITORY: mern-backend-repo
  IMAGE_NAME: backend

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:

    # 1. Checkout
    - name: Checkout repository
      uses: actions/checkout@v4

    # 2. Auth GCP (OIDC)
    - name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v2
      with:
        workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
        service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

    # 3. Setup gcloud
    - name: Setup Google Cloud SDK
      uses: google-github-actions/setup-gcloud@v2

    # 4. ✅ Installer gke-gcloud-auth-plugin (AJOUT ICI)
    - name: Install GKE auth plugin
      run: |
        gcloud components install gke-gcloud-auth-plugin --quiet

    # 5. Docker auth
    - name: Configure Docker authentication
      run: |
        gcloud auth configure-docker $ARTIFACT_REGISTRY --quiet

    # 6. Build image
    - name: Build Docker image
      run: |
        docker build \
          -t $ARTIFACT_REGISTRY/$PROJECT_ID/$REPOSITORY/$IMAGE_NAME:${{ github.sha }} \
          ./backend

    # 7. Push image
    - name: Push Docker image
      run: |
        docker push $ARTIFACT_REGISTRY/$PROJECT_ID/$REPOSITORY/$IMAGE_NAME:${{ github.sha }}

    # 8. Get GKE credentials
    - name: Get GKE credentials
      run: |
        gcloud container clusters get-credentials $GKE_CLUSTER \
          --zone $GKE_ZONE \
          --project $PROJECT_ID

    # 9. Install Helm
    - name: Install Helm
      run: |
        curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

    # 10. Deploy with Helm
    - name: Deploy with Helm
      run: |
        helm upgrade --install backend ./helm/backend \
          --namespace backend \
          --create-namespace \
          --set image.repository=$ARTIFACT_REGISTRY/$PROJECT_ID/$REPOSITORY/$IMAGE_NAME \
          --set image.tag=${{ github.sha }} \
          --wait \
          --atomic