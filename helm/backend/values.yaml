namespace: backend

replicaCount: 1

image:
  repository: europe-west1-docker.pkg.dev/model-nexus-496108-s7/mern-backend-repo/backend
  tag: v1.0.0
  pullPolicy: Always

service:
  type: ClusterIP
  port: 80
  targetPort: 5000

ingress:
  enabled: true
  className: nginx
  host: api.taskflow.fblouison.com

resources:
  requests:
    memory: "64Mi"
    cpu: "50m"

  limits:
    memory: "128Mi"
    cpu: "200m"

hpa:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  cpuUtilization: 70

config:
  PORT: "5000"
  NODE_ENV: production

secret:
  MONGO_URI: "mongodb+srv://taskflow_user:taskflow123!@taskflow.bcfm57b.mongodb.net/taskflow?retryWrites=true&w=majority"
  JWT_SECRET: "taskflow_secret_2024"